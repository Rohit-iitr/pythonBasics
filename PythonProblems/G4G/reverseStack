def modifyQueue(q, k):
    # code here
    print(q)
    stack = []
    for loopCounter in range(k):
        stack.append(q.pop(0))

    for loopCounter in range(k):
        q.insert(0, stack.pop(0))

    print(q)


modifyQueue([1, 2, 3, 4], 4)
